import React from "react";
import Header from "../app/layout/Header";

const Home = (props) => {

    return(
    <div>
        <Header
            section="home">
        </Header>
        <div>Welcome!!!</div>
    </div>
    );
};

export default Home;
